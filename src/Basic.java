

public class Basic {
    public static void main(String[] args) {

        
        if (args.length != 1){
            System.out.println("Error: There should at least or at most, 1 file passed!");
            System.exit(0);
        }
        // TODO : pass the filename into the lexer instance
    }
}
